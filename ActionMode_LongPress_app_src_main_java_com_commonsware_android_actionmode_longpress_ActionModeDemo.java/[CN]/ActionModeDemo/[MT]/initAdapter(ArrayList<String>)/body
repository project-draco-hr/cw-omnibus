{
  if (startingPoint == null) {
    words=new ArrayList<String>();
    for (int i=0; i < 5; i++) {
      words.add(items[i]);
    }
  }
 else {
    words=startingPoint;
  }
  adapter=new ArrayAdapter<String>(this,android.R.layout.simple_list_item_activated_1,words);
  setListAdapter(adapter);
}
