{
  updateButtons();
  if (requestCode == RESULT_PERMS_TAKE_PICTURE) {
    if (canTakePicture()) {
      takePictureForRealz();
    }
  }
 else   if (requestCode == RESULT_PERMS_RECORD_VIDEO) {
    if (canRecordVideo()) {
      recordVideoForRealz();
    }
  }
}
